{
  "name": "lyrix",
  "version": "1.0.0",
  "description": "LYRIX — AI Lyric Architect",
  "scripts": {
    "build": "echo 'Static site — no build step needed'",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
